file = 'sample.lua'
dir = '../templates/docs'
output = 'docs'
project = 'Bot Documentation'
template = "!snake"
style = false

-- examples are not well configured with django, so they willnot be used for now
-- examples = { '.', exclude = {'pl', 'ldoc'} }
